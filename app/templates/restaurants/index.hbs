<h3 class="text-info text-center">{{t "messages.bestRestaurants"}}</h3>

<div class="panel panel-default">
	<div class="panel-heading">
		<h3 class="panel-title">{{t "sections.restaurants"}}</h3>
	</div>

	<div class="list-group">
		{{#each model as |restaurant| }}
			{{#link-to 'restaurants.detail' (query-params id=restaurant.id) class="list-group-item"}}
				{{restaurant-listing restaurant=restaurant}}
			{{/link-to}}
		{{/each}}
	</div>
</div>
